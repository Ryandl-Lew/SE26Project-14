spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/bionote?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:bionote}
    password: ${DB_PASSWORD:bionote}
    driver-class-name: com.mysql.cj.jdbc.Driver

logging:
  level:
    com.bionote: DEBUG
    org.springframework.security: INFO
