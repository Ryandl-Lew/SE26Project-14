/**
 * 领域模型统一出口
 * 运行时导出：枚举标签 / 徽章色系映射（enums.js）。
 * 形状定义（models.js、common.js）为纯 JSDoc，无运行时导出。
 *
 * 使用：import { PROJECT_STATUS_LABELS, RECORD_STATUS_TONES } from '@/domain'
 */
export * from './enums'
